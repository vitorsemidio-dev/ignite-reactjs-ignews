export enum PrismicTypes {
  POST = "post",
}

export enum PrismicTypePost {
  UID = "uid",
  TITLE = "title",
  CONTENT = "content",
}

export enum PrismicPredicates {
  DOCUMENT_TYPE = "document.type",
}
